name: "Check Gersemi"
description: "Install gersemi via uv-tool and run gersemi --check"

runs:
  using: "composite"
  steps:

    - shell: pwsh
      run: |
        & gersemi --check .
        if ($LASTEXITCODE -ne 0) {
          exit $LASTEXITCODE
        }
