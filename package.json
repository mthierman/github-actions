{
    "name": "github-actions",
    "private": true,
    "scripts": {
        "build": "esbuild src/index.js --bundle --platform=node --target=node22 --format=cjs --outfile=hello-world-javascript-action/dist/index.js",
        "action:build": "esbuild hello-js/action.js --bundle --platform=node --target=node22 --format=cjs --outfile=hello-js/dist/action.js"
    },
    "devDependencies": {
        "@actions/core": "^1.11.1",
        "@actions/github": "^6.0.1",
        "esbuild": "^0.25.9",
        "prettier": "^3.6.2",
        "typescript": "^5.9.2"
    }
}
